# ===========================================
# CommitCamp — commitcamp.com
# ===========================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# Get these from your Supabase dashboard:
#   Settings > API
# ===========================================

# Supabase - Public (exposed to browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase - Secret (server-side only)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Site URL (for OAuth callbacks). Required for Discord OAuth to avoid "invalid OAuth" — must match Discord Portal redirect exactly.
# Local:  NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Prod:   NEXT_PUBLIC_SITE_URL=https://commitcamp.com
# NEXT_PUBLIC_SITE_URL=

# OAuth Providers (configured in Supabase Dashboard)
# GitHub: Settings > Authentication > Providers > GitHub
# Google: Settings > Authentication > Providers > Google
# Discord: Settings > Authentication > Providers > Discord

# Discord OAuth (for "Connect with Discord" messaging). Create app: https://discord.com/developers/applications
# In Discord: OAuth2 > Redirects — add EXACTLY (same as below, no trailing slash):
#   Local: http://localhost:3000/api/auth/discord/callback
#   Prod:  https://commitcamp.com/api/auth/discord/callback
# DISCORD_CLIENT_ID=your-discord-application-client-id
# DISCORD_CLIENT_SECRET=your-discord-application-client-secret

# Email (Resend) — optional
# Auth emails use Supabase Custom SMTP: put this key in Dashboard > Authentication > SMTP Settings > Password.
# Resend free tier: 3,000 emails/month (100/day). Same key can be used later for server-side emails.
# RESEND_API_KEY=re_your_api_key_here
