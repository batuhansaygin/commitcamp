# Platform app URL (Navbar CTAs, tool links). Local dev example:
PUBLIC_PLATFORM_URL=http://localhost:3000

# Marketing site canonical domain is fixed in astro.config.mjs (https://commitcamp.com)
# so sitemap, canonical, and JSON-LD stay correct even if this file uses localhost above.
